[package]
name = "peak-system"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.13", features = ["advanced"] }
peak-core = { path = "../peak-core" }
sysinfo = "0.30"
serde = { version = "1.0", features = ["derive"] }
